//Дан односвязный линейный список и указатель на голову списка P1. Необходимо вывести указатель на восьмой элемент этого списка P8. 
//Известно, что в исходном списке не менее 8 элементов.
#include "pt4.h"
using namespace std;

void Solve()
{
    Task("ListWork7");
    PNode ptr;
    int k=1;
    pt >> ptr;
    while (k!=8)
    {
        ptr = ptr->Next;
        k++;   
    }
    if (k == 8) {
        pt << ptr;
    }
}
